# Minimal dependencies for the local eval pipeline.
# This avoids installing notebook/app-only pins such as ipython.

accelerate>=0.26.0
huggingface-hub>=0.34.0,<1.0
numpy>=1.26
pandas>=2.0
python-dotenv>=1.0
safetensors>=0.4
tabulate>=0.9
torch
tqdm>=4.66
transformers>=4.45,<5
